# Dependências de produção — ARgent.ai backend
# Sem versões fixas: o pip escolhe wheels compatíveis com o Python do Render.
# Heavy/optional libs (chromadb, onnxruntime, qdrant) removidas — o RAG usa
# fallback no banco e funciona sem elas.

fastapi
uvicorn[standard]
sqlalchemy
pydantic
python-dotenv
python-multipart
python-jose[cryptography]
bcrypt
openai
requests
pypdf
psycopg2-binary
ddgs
